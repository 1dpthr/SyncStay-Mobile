rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Development: allow all access. Tighten before production.
      allow read, write: if true;
    }
  }
}
